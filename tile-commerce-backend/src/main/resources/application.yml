spring:
  application:
    name: tile-commerce-backend
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/tile_commerce?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Rasool@0467}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

server:
  port: 9195

app:
  jwt:
    secret: ${JWT_SECRET:VGlsZUNvbW1lcmNlX0pXVF8yMDI2X1NlY3VyZUtleV85MXhQN21RMnpMOA==}
    expiration-ms: ${JWT_EXPIRATION:86400000}
