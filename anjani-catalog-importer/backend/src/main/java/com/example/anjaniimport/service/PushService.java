package com.example.anjaniimport.service;

import org.springframework.stereotype.Service;

/**
 * Intentionally empty in the live-catalog build. Product insertion is performed by the
 * Tile Commerce backend after the authenticated shop owner selects products in the UI.
 */
@Service
public class PushService { }
